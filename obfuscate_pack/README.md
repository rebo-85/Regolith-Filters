# obfuscate_pack

Obfuscates JSON files in the staged `BP` and `RP` packs. It preserves fixed Bedrock filenames and stores stable source-to-obfuscated mappings in the addon project's `.obfuscation/map.json`.

Run this filter before `brarchive` when publishing obfuscated archives.
