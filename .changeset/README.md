# Changesets

Run `pnpm changeset` after changing a filter. Select only the affected filter
packages, commit the generated Markdown file, and include it in the pull
request. The release process consumes these files to calculate independent
package versions.